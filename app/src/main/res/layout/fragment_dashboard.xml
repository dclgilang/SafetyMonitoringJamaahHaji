<?xml version="1.0" encoding="utf-8"?>
<layout>


    <id.co.dif.smj.custom_view.LinearLayoutWithAppBar xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/root_layout"
        android:layout_width="match_parent"
        android:orientation="vertical"
        android:layout_height="wrap_content"
        app:actionButtonIcon="@drawable/ic_setting"
        app:actionButtonTint="@color/light_blue"
        app:title="@string/dashboard">

        <com.google.android.material.tabs.TabLayout
            android:id="@+id/tabLayout"

            android:layout_width="match_parent"
            android:layout_height="64dp"
            android:textSize="14sp"
            android:background="@color/white"
            android:theme="@style/text_bold_black"
            app:tabIndicatorColor="@color/light_blue"
            app:tabIndicatorHeight="10dp"
            app:tabSelectedTextColor="@color/light_blue"
            app:tabTextAppearance="@android:style/TextAppearance.Widget.TabWidget" />

        <androidx.viewpager.widget.ViewPager
            android:id="@+id/view_pager"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

    </id.co.dif.smj.custom_view.LinearLayoutWithAppBar>

</layout>